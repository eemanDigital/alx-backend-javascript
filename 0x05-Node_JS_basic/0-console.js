/**
 * Displays a message on the STDOUT.
 * @param {String} msg Reference the input to display.
 */
const displayMessage = (message) => {
  return console.log(message);
};

module.exports = displayMessage;
